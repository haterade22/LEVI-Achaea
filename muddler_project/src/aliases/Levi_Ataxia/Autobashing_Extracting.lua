if not matches[1]:find("m") then
	if ataxiaBasher.enabled then
		disable_extractor()
	else
		enable_extractor()
	end
else
	if ataxiaBasher.enabled then
		disable_extractor()
	else
		enable_extractorm()
	end
end

